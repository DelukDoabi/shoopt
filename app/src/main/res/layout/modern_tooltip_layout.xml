<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/tooltip_card"
    android:layout_width="340dp"
    android:layout_height="wrap_content"
    android:layout_margin="16dp"
    android:elevation="16dp"
    app:cardCornerRadius="20dp"
    app:cardBackgroundColor="#2D2D2D">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <!-- Accent line moderne -->
        <View
            android:layout_width="60dp"
            android:layout_height="4dp"
            android:layout_marginBottom="20dp"
            android:background="@drawable/tooltip_accent_modern"
            android:alpha="0.8" />

        <!-- Icon moderne - taille réduite -->
        <androidx.cardview.widget.CardView
            android:id="@+id/tooltip_icon_card"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginBottom="16dp"
            app:cardCornerRadius="20dp"
            app:cardElevation="6dp"
            app:cardBackgroundColor="@color/main_palette_old_lavender">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:src="@android:drawable/ic_input_add"
                app:tint="@android:color/white"
                android:contentDescription="Add product icon" />
        </androidx.cardview.widget.CardView>

        <!-- Tooltip Title -->
        <TextView
            android:id="@+id/tooltip_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end"
            android:shadowColor="#40000000"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="2"
            tools:text="Bienvenue dans Shoopt !" />

        <!-- Description - full content display without scrolling -->
        <TextView
            android:id="@+id/tooltip_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:fontFamily="sans-serif"
            android:gravity="center"
            android:lineSpacingExtra="3dp"
            android:textColor="#E0E0E0"
            android:textSize="15sp"
            android:alpha="0.9"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            tools:text="Commencez par ajouter votre premier produit en cliquant ici. Vous pourrez scanner son code-barres ou l'ajouter manuellement." />

        <!-- Modern Gradient Button -->
        <androidx.cardview.widget.CardView
            android:id="@+id/tooltip_confirm_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            app:cardCornerRadius="25dp"
            app:cardElevation="6dp"
            app:cardBackgroundColor="@color/main_palette_old_lavender"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?android:attr/selectableItemBackground">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:orientation="horizontal"
                android:gravity="center"
                android:padding="4dp">

                <ImageView
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_marginEnd="8dp"
                    android:src="@android:drawable/ic_media_play"
                    app:tint="@android:color/white"
                    android:contentDescription="Start icon" />

                <TextView
                    android:id="@+id/tooltip_confirm_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="sans-serif-medium"
                    android:textColor="@android:color/white"
                    android:textSize="16sp"
                    android:textAllCaps="false"
                    android:textStyle="bold"
                    tools:text="Commencer !" />

            </LinearLayout>

        </androidx.cardview.widget.CardView>

        <!-- Modern Skip Button -->
        <LinearLayout
            android:id="@+id/tooltip_skip_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:padding="12dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?android:attr/selectableItemBackgroundBorderless">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginEnd="6dp"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                app:tint="#B0B0B0"
                android:contentDescription="Skip icon" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif"
                android:textColor="#B0B0B0"
                android:textSize="14sp"
                android:text="Passer le guide" />

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
